= form_for(:user_session, :url => '/user_session') do |f|
  .field
    = f.label :login
    = f.text_field :login
  .field
    = f.label :password
    = f.password_field :password
  .actions
    = f.submit 'login'
%p#signup= link_to 'Signup for a new account', new_user_path
